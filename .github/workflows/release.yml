name: Release

on:
  # Three ways in, all landing on the same `prepare` job:
  #   - a commit on main that changes the version in package.json (the normal path),
  #   - pushing a `v*` tag,
  #   - a manual dispatch, to rebuild a release that already exists.
  push:
    branches: [main]
    tags: ['v*']
  workflow_dispatch:
    inputs:
      release_tag:
        description: Tag to build, for example v1.2.4. Defaults to the version in package.json.
        required: false
        type: string
  release:
    types: [published]
permissions:
  contents: write

jobs:
  prepare:
    name: Resolve release
    runs-on: ubuntu-latest
    outputs:
      tag: ${{ steps.resolve.outputs.tag }}
      proceed: ${{ steps.resolve.outputs.proceed }}

    steps:
      - name: Check out repository
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Resolve tag and create the release
        id: resolve
        shell: bash
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          EVENT_NAME: ${{ github.event_name }}
          REF_NAME: ${{ github.ref_name }}
          REF_TYPE: ${{ github.ref_type }}
          INPUT_TAG: ${{ inputs.release_tag }}
          EVENT_TAG: ${{ github.event.release.tag_name }}
        run: |
          set -euo pipefail

          # The tag under discussion, whichever way this run started.
          case "$EVENT_NAME" in
            workflow_dispatch) tag="$INPUT_TAG" ;;
            release) tag="$EVENT_TAG" ;;
            push) test "$REF_TYPE" = tag && tag="$REF_NAME" || tag="" ;;
            *) tag="" ;;
          esac
          if test -z "$tag"; then
            tag="v$(node -p "require('./package.json').version")"
          fi
          echo "tag=$tag" >> "$GITHUB_OUTPUT"
          echo "Release tag: $tag"

          # An ordinary commit on main is not a release. Only the one that lands a version
          # nobody has released yet is, which is what makes "bump, commit, push" enough.
          if gh release view "$tag" --repo "$GITHUB_REPOSITORY" >/dev/null 2>&1; then
            if test "$EVENT_NAME" = push && test "$REF_TYPE" = branch; then
              echo "$tag is already released; nothing to do."
              echo "proceed=false" >> "$GITHUB_OUTPUT"
              exit 0
            fi
            echo "proceed=true" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          if ! git rev-parse -q --verify "refs/tags/$tag" >/dev/null; then
            git config user.name "github-actions[bot]"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
            git tag -a "$tag" -m "Release ${tag#v}"
            git push origin "$tag"
          fi

          # Notes come from the annotated tag, else the release commit's own message — the
          # place the change is already described — and only then from GitHub's generator.
          notes="$RUNNER_TEMP/release-notes.md"
          git tag -l --format='%(contents:body)' "$tag" > "$notes"
          if ! test -s "$notes"; then
            git log -1 --format='%b' "$tag^{commit}" | grep -v '^Co-Authored-By:' > "$notes" || true
          fi

          if test -s "$notes"; then
            gh release create "$tag" --repo "$GITHUB_REPOSITORY" \
              --title "Neno Music ${tag#v}" --notes-file "$notes"
          else
            gh release create "$tag" --repo "$GITHUB_REPOSITORY" \
              --title "Neno Music ${tag#v}" --generate-notes
          fi
          echo "proceed=true" >> "$GITHUB_OUTPUT"

  release:
    name: Build ${{ matrix.platform.name }}
    needs: prepare
    if: needs.prepare.outputs.proceed == 'true'
    env:
      RELEASE_TAG: ${{ needs.prepare.outputs.tag }}
    strategy:
      fail-fast: false
      matrix:
        platform:
          - name: Windows
            os: windows-latest
            rust_target: ""
            build_args: --bundles msi,nsis
            apple_signing_identity: ""
          - name: macOS Apple Silicon
            os: macos-latest
            rust_target: aarch64-apple-darwin
            build_args: --target aarch64-apple-darwin --bundles app,dmg
            apple_signing_identity: "-"
          - name: macOS Intel
            os: macos-15-intel
            rust_target: x86_64-apple-darwin
            build_args: --target x86_64-apple-darwin --bundles app,dmg
            apple_signing_identity: "-"
          - name: Linux
            os: ubuntu-latest
            rust_target: ""
            build_args: --bundles deb,rpm,appimage
            apple_signing_identity: ""
    runs-on: ${{ matrix.platform.os }}

    steps:
      - name: Verify release exists
        shell: bash
        run: gh release view "$RELEASE_TAG" --repo "$GITHUB_REPOSITORY"
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Read release type
        id: release_metadata
        shell: bash
        run: |
          set -euo pipefail
          is_prerelease="$(gh release view "$RELEASE_TAG" \
            --repo "$GITHUB_REPOSITORY" \
            --json isPrerelease \
            --jq .isPrerelease)"

          if test "$is_prerelease" = "true"; then
            echo "publish_update=false" >> "$GITHUB_OUTPUT"
          else
            echo "publish_update=true" >> "$GITHUB_OUTPUT"
          fi
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Check out repository
        uses: actions/checkout@v7
        with:
          ref: ${{ env.RELEASE_TAG }}
          fetch-depth: 0

      - name: Verify checked out tag
        shell: bash
        run: |
          set -euo pipefail
          test "$(git describe --tags --exact-match)" = "$RELEASE_TAG"

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Add Rust target
        if: matrix.platform.rust_target != ''
        run: rustup target add ${{ matrix.platform.rust_target }}

      - name: Install Linux system dependencies
        if: matrix.platform.name == 'Linux'
        shell: bash
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev \
            build-essential \
            curl \
            wget \
            file \
            libssl-dev \
            libayatana-appindicator3-dev \
            librsvg2-dev \
            libgtk-3-dev \
            libsoup-3.0-dev \
            libjavascriptcoregtk-4.1-dev \
            libasound2-dev \
            libdbus-1-dev \
            cmake \
            patchelf

      - name: Cache Rust build
        uses: swatinem/rust-cache@v2
        with:
          workspaces: "./src-tauri -> target"

      - name: Install frontend dependencies
        run: npm ci

      - name: Build and upload installers
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          APPLE_SIGNING_IDENTITY: ${{ matrix.platform.apple_signing_identity }}
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}
        with:
          tagName: ${{ env.RELEASE_TAG }}
          releaseDraft: false
          uploadUpdaterJson: ${{ steps.release_metadata.outputs.publish_update }}
          uploadUpdaterSignatures: false
          updaterJsonPreferNsis: true
          args: ${{ matrix.platform.build_args }}

      - name: Upload Windows portable executable
        if: matrix.platform.name == 'Windows'
        shell: bash
        run: |
          set -euo pipefail
          # Located rather than hardcoded: the executable is named from the Cargo package
          # (`zuno`), which Tauri may rename to `productName` during bundling. Globbing the
          # one top-level exe survives either, and fails loudly if that assumption breaks.
          portable_exe=$(find src-tauri/target/release -maxdepth 1 -type f -name '*.exe' | head -n 1)
          if [ -z "$portable_exe" ]; then
            echo "No portable executable found in src-tauri/target/release" >&2
            exit 1
          fi
          echo "Uploading $portable_exe"
          gh release upload "$RELEASE_TAG" \
            "${portable_exe}#Neno-Music-Windows-portable.exe" \
            --clobber
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Verify macOS DMG and app signature
        if: startsWith(matrix.platform.name, 'macOS')
        shell: bash
        run: |
          set -euo pipefail

          bundle_dir="src-tauri/target/${{ matrix.platform.rust_target }}/release/bundle/dmg"
          dmg_path="$(find "$bundle_dir" -maxdepth 1 -name '*.dmg' -print -quit)"
          test -n "$dmg_path"
          hdiutil verify "$dmg_path"

          mount_dir="$(mktemp -d)"
          cleanup() {
            hdiutil detach "$mount_dir" -quiet || true
            rmdir "$mount_dir" || true
          }
          trap cleanup EXIT

          hdiutil attach "$dmg_path" -readonly -nobrowse -mountpoint "$mount_dir"
          app_path="$(find "$mount_dir" -maxdepth 1 -name '*.app' -print -quit)"
          test -n "$app_path"
          codesign --verify --deep --strict --verbose=2 "$app_path"

  build-android:
    name: Build Android APK
    needs: prepare
    if: needs.prepare.outputs.proceed == 'true'
    runs-on: ubuntu-latest
    env:
      RELEASE_TAG: ${{ needs.prepare.outputs.tag }}
    steps:
      - name: Check out repository
        uses: actions/checkout@v7
        with:
          ref: ${{ env.RELEASE_TAG }}
          fetch-depth: 0

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Install NDK
        run: |
          sdkmanager --install "ndk;26.1.10909125"

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: aarch64-linux-android,armv7-linux-androideabi,i686-linux-android,x86_64-linux-android

      - name: Cache Rust build
        uses: swatinem/rust-cache@v2
        with:
          workspaces: "./src-tauri -> target"
          key: android

      - name: Install dependencies
        run: npm ci

      - name: Set NDK Environment Variables
        run: |
          echo "NDK_HOME=$ANDROID_HOME/ndk/26.1.10909125" >> $GITHUB_ENV
          echo "ANDROID_NDK_HOME=$ANDROID_HOME/ndk/26.1.10909125" >> $GITHUB_ENV

      - name: Build Android APKs
        run: |
          npm run build
          npx tauri android build --apk

      - name: Collect and Rename Android APKs
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p dist-apks
          echo "Locating built APK files..."
          find src-tauri/gen/android/app/build/outputs/apk -name "*.apk" -type f
          
          # Universal APK
          universal_apk=$(find src-tauri/gen/android/app/build/outputs/apk -name "*universal*release*.apk" -type f | head -n 1)
          if [ -n "$universal_apk" ]; then
            cp "$universal_apk" "dist-apks/Neno-Music-${RELEASE_TAG}-Android-universal.apk"
          fi

          # arm64-v8a APK
          arm64_apk=$(find src-tauri/gen/android/app/build/outputs/apk -name "*arm64*release*.apk" -type f | head -n 1)
          if [ -n "$arm64_apk" ]; then
            cp "$arm64_apk" "dist-apks/Neno-Music-${RELEASE_TAG}-Android-arm64.apk"
          fi

          # Fallback: copy any release APK found
          if [ ! -f "dist-apks/Neno-Music-${RELEASE_TAG}-Android-universal.apk" ]; then
            for apk in $(find src-tauri/gen/android/app/build/outputs/apk -name "*.apk" -type f); do
              base=$(basename "$apk")
              cp "$apk" "dist-apks/Neno-Music-${RELEASE_TAG}-Android-${base}"
            done
          fi

      - name: Upload Android APKs to GitHub Release
        shell: bash
        run: |
          for apk in dist-apks/*.apk; do
            if [ -f "$apk" ]; then
              echo "Uploading $apk to release $RELEASE_TAG"
              gh release upload "$RELEASE_TAG" "$apk" --clobber
            fi
          done
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  build-ios:
    name: Build iOS App
    needs: prepare
    if: needs.prepare.outputs.proceed == 'true'
    runs-on: macos-15
    env:
      RELEASE_TAG: ${{ needs.prepare.outputs.tag }}
    steps:
      - name: Check out repository
        uses: actions/checkout@v7
        with:
          ref: ${{ env.RELEASE_TAG }}
          fetch-depth: 0

      - name: Set up Xcode
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: latest-stable

      - name: Setup CMake policy wrapper
        run: |
          mkdir -p "$HOME/cmake-bin"
          if [ -f "scripts/cmake-wrapper.sh" ]; then
            cp scripts/cmake-wrapper.sh "$HOME/cmake-bin/cmake"
            chmod +x "$HOME/cmake-bin/cmake"
            echo "$HOME/cmake-bin" >> "$GITHUB_PATH"
          fi

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Set up Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: aarch64-apple-ios,aarch64-apple-ios-sim,x86_64-apple-ios

      - name: Rust Cache
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: src-tauri
          key: ios

      - name: Install Node dependencies
        run: npm ci

      - name: Initialize Tauri iOS project (if needed)
        run: |
          if [ ! -d "src-tauri/gen/apple" ]; then
            echo "Initializing Tauri iOS project..."
            npx tauri ios init
          else
            echo "Tauri iOS project already exists."
          fi

      - name: Build Web Frontend
        run: npm run build

      - name: Build Tauri iOS App
        run: |
          npx tauri ios build --export-method debugging --no-sign || true

      - name: Prepare and Rename iOS Artifacts
        run: |
          mkdir -p dist-ios
          echo "Searching for compiled .ipa and .app files:"
          find src-tauri/gen/apple -name "*.ipa" -exec cp {} "dist-ios/Neno-Music-${RELEASE_TAG}-iOS.ipa" \; || true
          zip -r "dist-ios/Neno-Music-${RELEASE_TAG}-iOS-Xcode.zip" src-tauri/gen/apple -x "*/DerivedData/*" || true

      - name: Upload iOS Assets to GitHub Release
        shell: bash
        run: |
          for item in dist-ios/*; do
            if [ -f "$item" ]; then
              echo "Uploading $item to release $RELEASE_TAG"
              gh release upload "$RELEASE_TAG" "$item" --clobber
            fi
          done
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  publish-updater-channel:
    name: Publish gated updater metadata
    needs: [prepare, release]
    runs-on: ubuntu-latest
    env:
      RELEASE_TAG: ${{ needs.prepare.outputs.tag }}

    steps:
      - name: Check out repository
        uses: actions/checkout@v7
        with:
          ref: ${{ env.RELEASE_TAG }}
          fetch-depth: 0

      - name: Read release type
        id: release_metadata
        shell: bash
        run: |
          set -euo pipefail
          is_prerelease="$(gh release view "$RELEASE_TAG" \
            --repo "$GITHUB_REPOSITORY" \
            --json isPrerelease \
            --jq .isPrerelease)"

          if test "$is_prerelease" = "true"; then
            echo "publish_update=false" >> "$GITHUB_OUTPUT"
          else
            echo "publish_update=true" >> "$GITHUB_OUTPUT"
          fi
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Download generated updater metadata
        if: steps.release_metadata.outputs.publish_update == 'true'
        shell: bash
        run: |
          set -euo pipefail
          gh release download "$RELEASE_TAG" \
            --repo "$GITHUB_REPOSITORY" \
            --pattern latest.json \
            --dir "$RUNNER_TEMP/updater-channel"
          test -s "$RUNNER_TEMP/updater-channel/latest.json"
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Publish updater channel
        if: steps.release_metadata.outputs.publish_update == 'true'
        shell: bash
        run: |
          set -euo pipefail

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          if git ls-remote --exit-code --heads origin updater-channel >/dev/null 2>&1; then
            git fetch origin updater-channel
            git switch --force-create updater-channel origin/updater-channel
          else
            git switch --orphan updater-channel
          fi

          git rm -rf --ignore-unmatch .
          cp "$RUNNER_TEMP/updater-channel/latest.json" latest.json
          git add latest.json

          if git diff --cached --quiet; then
            echo "Updater channel already points to $RELEASE_TAG."
            exit 0
          fi

          git commit -m "Offer $RELEASE_TAG through the updater"
          git push origin updater-channel

  publish-winget:
    name: Publish to winget
    needs: [prepare, release]
    runs-on: ubuntu-latest
    # `winget-releaser` only *bumps* a package that already exists in microsoft/winget-pkgs; it
    # cannot create one. `noFAYZ.Zuno` has never been submitted, so every run so far has stopped
    # at "Package noFAYZ.Zuno does not exist in the winget-pkgs repository" — nothing to do with
    # the release itself, which published fine each time.
    #
    # The initial submission is a one-off PR to microsoft/winget-pkgs; the manifest for it lives
    # in `manifests/`. This step starts working on its own once that PR is merged.
    #
    # Non-fatal until then. winget is one channel among several, and failing the whole release
    # for it also meant the AUR publish — which chains off this workflow succeeding — never ran.
    continue-on-error: true
    env:
      RELEASE_TAG: ${{ needs.prepare.outputs.tag }}

    steps:
      - name: Read release type
        id: release_metadata
        shell: bash
        run: |
          set -euo pipefail
          gh release view "$RELEASE_TAG" --repo "$GITHUB_REPOSITORY" \
            --json isPrerelease --jq '"prerelease=" + (.isPrerelease | tostring)' \
            >> "$GITHUB_OUTPUT"
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # Version defaults to the release tag with the leading `v` stripped.
      - uses: vedantmgoyal9/winget-releaser@main
        if: steps.release_metadata.outputs.prerelease == 'false'
        with:
          identifier: noFAYZ.Zuno
          release-tag: ${{ env.RELEASE_TAG }}
          installers-regex: '_x64-setup\.exe$'
          token: ${{ secrets.WINGET_TOKEN }}
